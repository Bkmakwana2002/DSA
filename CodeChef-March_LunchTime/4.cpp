// https://www.codechef.com/LTIME106C/problems/ADJLOVE
#include <bits/stdc++.h>
using namespace std;
#define ll long long int

ll reArrangeArray(ll arr[], int ll, ll oddCount, ll evenCount)
{
    if (abs(oddCount - evenCount) > 1)
    {
        return -1;
    }
    if (oddCount > evenCount)
    {
        ll i = 0;
        while(oddCount--)
        {
          
        }
    }
}

int main()
{
    ll t;
    while (t--)
    {
        ll n;
        cin >> n;
        ll arr[n];
        for (ll i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
        
    }
    return 0;
}